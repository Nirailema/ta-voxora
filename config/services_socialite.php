<?php

/**
 * Socialite is configured via config/services.php which reads
 * GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, and GOOGLE_REDIRECT_URI
 * from environment variables.
 *
 * No additional config file needed — Laravel Socialite auto-detects
 * these keys from config/services.php under the 'google' key.
 *
 * To publish Socialite's default config (optional):
 *   php artisan vendor:publish --tag=socialite-config
 */
